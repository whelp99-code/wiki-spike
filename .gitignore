# secrets / local config
.env
*.key
signing.key

# runtime artifacts (control-plane, git repos, CAS)
*.sqlite
*.sqlite-*
control.sqlite*
repo.git/
remote.git/
cas/
.wiki-spike/

# shadow-measurement cohort state: signing keys, growing journals, locks.
# Archived dead cohorts are retained on disk as signed evidence but are
# never tracked.
artifacts/second-brain/operational-cohort/
artifacts/second-brain/cohort-dead-*/

# agent/tool session state
.gjc/
.omo/

# python
__pycache__/
*.pyc
*.egg-info/
build/
dist/
.pytest_cache/
.venv/

# local CI/preflight logs
.ci/

# macOS
.DS_Store
